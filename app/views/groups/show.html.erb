<%= render "budgets/header" %>
<div class="d-flex flex-column justify-content-center align-items-center m-3 pt-3">
  <div class="card d-flex flex-column justify-content-center align-items-center m-3 pt-3" style="width: 18rem;">
    <div>
    <% if @group.icon %>
    <span class="material-symbols-outlined img">
      <%= @group.icon %>
      </span>
  </div>
  <% end %>
  <div class="card-body d-flex flex-column justify-content-center align-items-center">
    <h5 class="card-title badge text-bg-warning p-2 px-4"><%= @group.name %></h5>
    <p class="card-text">Total Amount: $ <%= @group.budgets.map{|b| b.amount}.sum %></p>
    <p class="card-text opacity-50"><%= time_ago_in_words(@group[:created_at]) %></p>
  </div>
</div>
<%= link_to "Add New Budget", new_user_budget_path(current_user.id), class: "btn btn-success mb-5" %>
<% if @group.budgets.any? %>
    <h1>All Added Budgets</h1>
    <% @budgets.each do |budget|%>
      <%= render 'budgets/budget_card', budget:budget %>
    <% end %>
  <% else %>
    <h1>No Budgets Yet</h1>
<% end %>